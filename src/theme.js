import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#d3642d" },
    secondary: { main: "#5a0f0f" },
    tertiary: { main: "#f0dc9d" },
    colorTextHeader: { main: "#64748b" },
    colorDefault: { main: "#ffffff" },
    colorTitle: { main: "#1E293B" },
  },
});

export default theme;
